# 积分系统技术文档

## 📋 概述

基于订阅的积分系统，将积分管理与现有订阅系统深度整合。用户订阅不同套餐时自动获得相应积分，简化支付流程和系统复杂度。

## 🏗️ 系统架构

### 核心设计理念
- **统一支付模式**: 取消独立积分购买，订阅即获得积分
- **自动化管理**: 订阅和续费时自动发放积分
- **按周期发放**: 每个计费周期自动补充积分
- **简化用户体验**: 一次订阅，获得服务+积分

### 技术栈
- **后端**: Next.js App Router + Drizzle ORM + PostgreSQL
- **前端**: React + TypeScript + Tailwind CSS
- **支付**: Stripe Webhooks
- **认证**: Better Auth

## 🗄️ 数据库设计

### 表结构

#### 1. user_credits - 用户积分账户表
```sql
CREATE TABLE user_credits (
  id TEXT PRIMARY KEY,
  user_id TEXT UNIQUE NOT NULL REFERENCES user(id),
  balance INTEGER NOT NULL DEFAULT 0,           -- 当前积分余额
  total_earned INTEGER NOT NULL DEFAULT 0,      -- 累计获得积分
  total_spent INTEGER NOT NULL DEFAULT 0,       -- 累计消费积分
  frozen_balance INTEGER NOT NULL DEFAULT 0,    -- 冻结积分
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 2. credit_transactions - 积分交易记录表
```sql
CREATE TABLE credit_transactions (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL REFERENCES user(id),
  type TEXT NOT NULL CHECK (type IN ('earn', 'spend', 'refund', 'admin_adjust', 'freeze', 'unfreeze')),
  amount INTEGER NOT NULL,                      -- 交易金额
  balance_after INTEGER NOT NULL,              -- 交易后余额
  source TEXT NOT NULL CHECK (source IN ('subscription', 'api_call', 'admin', 'storage', 'bonus')),
  description TEXT,                            -- 交易描述
  reference_id TEXT,                          -- 关联ID（如订阅ID）
  metadata TEXT,                              -- JSON格式的元数据
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 3. user_quota_usage - 用户配额使用表
```sql
CREATE TABLE user_quota_usage (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL REFERENCES user(id),
  service TEXT NOT NULL CHECK (service IN ('api_call', 'storage', 'custom')),
  period TEXT NOT NULL,                       -- 格式: YYYY-MM
  used_amount INTEGER NOT NULL DEFAULT 0,     -- 已使用量
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE(user_id, service, period)
);
```

## ⚙️ 核心服务

### CreditService 类

位置: `src/lib/credits/credit-service.ts`

#### 主要方法

##### 1. 创建积分账户
```typescript
async createCreditAccount(userId: string): Promise<UserCreditAccount>
```
- 为新用户创建积分账户
- 初始余额为0

##### 2. 赚取积分
```typescript
async earnCredits(params: EarnCreditsParams): Promise<CreditTransaction>
```
- 增加用户积分余额
- 记录交易历史
- 支持事务操作

##### 3. 消费积分
```typescript
async spendCredits(params: SpendCreditsParams): Promise<CreditTransaction>
```
- 扣除用户积分
- 检查余额充足性
- 记录消费记录

##### 4. 余额检查
```typescript
async hasEnoughCredits(userId: string, amount: number): Promise<boolean>
```
- 检查用户是否有足够积分
- 考虑冻结积分

##### 5. 交易历史
```typescript
async getTransactionHistory(userId: string, limit?: number, offset?: number): Promise<CreditTransaction[]>
```
- 获取用户交易历史
- 支持分页

## 🔗 系统集成

### 1. 订阅系统集成

#### Stripe Webhook 处理
位置: `src/app/api/webhooks/stripe/route.ts`

##### 订阅创建时发放积分
```typescript
// checkout.session.completed 事件
async function handleCheckoutSessionCompleted(event: StripeTypes.Event) {
  // ... 现有逻辑
  
  // 发放订阅积分
  const isYearly = price.recurring?.interval === 'year';
  await grantSubscriptionCredits(userId, priceId, subscriptionId, isYearly);
}
```

##### 每月续费时发放积分
```typescript
// invoice.paid 事件
async function handleInvoicePaid(event: StripeTypes.Event) {
  // ... 现有逻辑
  
  // 跳过首次付款，发放月度积分
  const isFirstPayment = invoice.billing_reason === 'subscription_create';
  if (!isFirstPayment && paymentRecord.userId) {
    await grantMonthlyCredits(userId, priceId, subscriptionId, invoice.id);
  }
}
```

### 2. 用户注册集成

#### Better Auth 钩子
位置: `src/lib/auth/auth.ts`

```typescript
hooks: {
  after: [
    {
      matcher(context) {
        return context.path === '/sign-up/email' && context.method === 'POST';
      },
      handler: async (ctx) => {
        if (ctx.returned?.user) {
          const userId = ctx.returned.user.id;
          
          // 创建积分账户
          await creditService.createCreditAccount(userId);
          
          // 发放注册奖励积分
          const freePlan = paymentConfig.plans.find(p => p.id === 'free');
          const signupCredits = freePlan?.credits?.onSignup;
          
          if (signupCredits && signupCredits > 0) {
            await creditService.earnCredits({
              userId,
              amount: signupCredits,
              source: 'bonus',
              description: 'Welcome bonus credits',
              referenceId: `signup_${userId}`,
            });
          }
        }
      },
    },
  ],
}
```

### 3. 定时任务

#### 月度免费积分发放
位置: `src/server/cron/monthly-credits.ts`

```typescript
export async function grantMonthlyFreeCredits() {
  // 获取所有免费用户
  const freeUsers = await db
    .select({ userId: user.id })
    .from(user)
    .leftJoin(payment, eq(payment.userId, user.id))
    .where(
      or(
        isNull(payment.status),
        not(inArray(payment.status, ['active', 'trialing']))
      )
    );

  // 为每个免费用户发放积分
  const freeCredits = paymentConfig.plans.find(p => p.id === 'free')?.credits?.monthly || 100;
  
  for (const user of freeUsers) {
    await creditService.earnCredits({
      userId: user.userId,
      amount: freeCredits,
      source: 'subscription',
      description: 'Monthly free credits',
      referenceId: `free_${new Date().toISOString().slice(0, 7)}`,
    });
  }
}
```

#### API 端点
位置: `src/app/api/cron/monthly-credits/route.ts`

```typescript
export async function GET() {
  const result = await grantMonthlyFreeCredits();
  return NextResponse.json(result);
}
```

## 📊 配置管理

### 1. 支付计划配置
位置: `src/config/payment.config.ts`

```typescript
export const paymentConfig: PaymentConfig = {
  plans: [
    {
      id: 'free',
      name: 'Free',
      price: 0,
      credits: {
        monthly: 100,    // 每月免费积分
        onSignup: 100,   // 注册赠送积分
      },
      // ...其他配置
    },
    {
      id: 'pro',
      name: 'Pro',
      price: 49,
      credits: {
        monthly: 1000,     // 每月积分
        yearly: 12000,     // 年付积分
        onSubscribe: 1000, // 订阅时立即获得
      },
      // ...其他配置
    },
    // ...更多计划
  ],
};
```

### 2. 积分消费配置
位置: `src/config/credits.config.ts`

```typescript
export const creditsConfig: CreditsConfig = {
  enabled: true,
  currency: 'credits',
  
  // 消费规则
  consumption: {
    apiCall: {
      costPerCall: 1,        // 每次API调用消耗1积分
      freeQuotaCalls: 0,     // 付费用户无免费配额
    },
    storage: {
      costPerGBPerMonth: 10, // 每GB每月消耗10积分
      freeQuotaGB: 0,        // 付费用户无免费配额
    },
  },
  
  // 免费用户配额
  freeUser: {
    apiCall: {
      freeQuotaCalls: 100,   // 免费用户每月100次免费调用
    },
    storage: {
      freeQuotaGB: 1,        // 免费用户1GB免费存储
    },
  },
};
```

## 🎨 前端组件

### 1. 积分余额组件
位置: `src/components/credits/credit-balance.tsx`

功能:
- 显示可用积分、总余额、冻结积分
- 显示累计获得积分
- 刷新功能

### 2. 积分历史组件
位置: `src/components/credits/credit-history.tsx`

功能:
- 显示交易历史记录
- 按类型显示不同图标和颜色
- 支持分页加载

### 3. 配额概览组件
位置: `src/components/credits/quota-overview.tsx`

功能:
- 显示API调用和存储使用情况
- 进度条显示使用率
- 区分免费配额和付费配额

### 4. 定价页面集成
位置: `src/components/blocks/pricing/pricing.tsx`

新增功能:
- 积分信息徽章
- 突出显示每月积分数量
- 订阅奖励积分显示

## 📱 Server Actions

### 积分相关操作
位置: `src/server/actions/credit-actions.ts`

#### 1. 获取积分余额
```typescript
export async function getCreditBalance(): Promise<ActionResult<GetCreditBalanceResponse>>
```

#### 2. 获取交易历史
```typescript
export async function getCreditHistory(params?: GetCreditHistoryParams): Promise<ActionResult<CreditTransaction[]>>
```

#### 3. 获取配额使用情况
```typescript
export async function getQuotaUsage(): Promise<ActionResult<GetQuotaUsageResponse>>
```

#### 4. 消费积分
```typescript
export async function spendCredits(
  amount: number,
  service: 'api_call' | 'storage',
  description?: string,
  referenceId?: string
): Promise<ActionResult<CreditTransaction>>
```

#### 5. 管理员授予积分
```typescript
export async function grantCreditsToUser(
  userId: string,
  amount: number,
  description?: string
): Promise<ActionResult<CreditTransaction>>
```

## 🛠️ 使用方式

### 1. 开发环境设置

#### 数据库迁移
```bash
# 生成迁移文件
npx drizzle-kit generate

# 应用迁移
npx drizzle-kit push
```

#### 环境变量
确保 `.env` 文件包含:
```env
DATABASE_URL=your_database_url
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=your_webhook_secret
```

### 2. API 使用示例

#### 在组件中获取积分余额
```typescript
'use client';

import { useEffect, useState } from 'react';
import { getCreditBalance } from '@/server/actions/credit-actions';

export function MyComponent() {
  const [credits, setCredits] = useState(0);

  useEffect(() => {
    async function loadCredits() {
      const result = await getCreditBalance();
      if (result.success) {
        setCredits(result.data.balance);
      }
    }
    loadCredits();
  }, []);

  return <div>Current Credits: {credits}</div>;
}
```

#### 消费积分
```typescript
import { spendCredits } from '@/server/actions/credit-actions';

async function handleApiCall() {
  const result = await spendCredits(
    1,           // 消费1积分
    'api_call',  // 服务类型
    'AI API call', // 描述
    `api_${Date.now()}` // 参考ID
  );
  
  if (result.success) {
    console.log('Credits spent successfully');
  } else {
    console.error('Insufficient credits');
  }
}
```

### 3. 管理员操作

#### 手动发放积分
```typescript
import { grantCreditsToUser } from '@/server/actions/credit-actions';

async function grantBonusCredits(userId: string) {
  const result = await grantCreditsToUser(
    userId,
    500,
    'Bonus credits for beta testing'
  );
  
  if (result.success) {
    console.log('Credits granted successfully');
  }
}
```

#### 触发月度积分发放
```bash
curl -X POST http://localhost:3000/api/cron/monthly-credits
```

### 4. 定时任务设置

#### Vercel Cron Jobs
在 `vercel.json` 中添加:
```json
{
  "crons": [
    {
      "path": "/api/cron/monthly-credits",
      "schedule": "0 2 1 * *"
    }
  ]
}
```

#### GitHub Actions (备选)
```yaml
name: Monthly Credits
on:
  schedule:
    - cron: '0 2 1 * *'
jobs:
  grant-credits:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger monthly credits
        run: |
          curl -X POST ${{ secrets.APP_URL }}/api/cron/monthly-credits
```

## 🔒 安全考虑

### 1. 权限控制
- 所有积分操作都需要用户认证
- 管理员操作需要额外权限验证
- 使用 Better Auth 的会话管理

### 2. 数据完整性
- 使用数据库事务确保积分操作的原子性
- 外键约束保证数据一致性
- 积分余额不能为负数

### 3. 防重放攻击
- Stripe webhook 使用签名验证
- 事件去重处理
- 参考ID确保操作唯一性

## 📊 监控与日志

### 1. 日志记录
- 所有积分交易都有详细日志
- Webhook 处理状态记录
- 错误日志包含上下文信息

### 2. 监控指标
- 积分发放成功率
- 消费模式分析
- 余额分布统计

## 🚀 部署指南

### 1. 生产环境检查清单
- [ ] 数据库迁移已应用
- [ ] 环境变量已配置
- [ ] Stripe webhook 端点已设置
- [ ] 定时任务已配置
- [ ] 日志监控已启用

### 2. 性能优化
- 数据库索引优化
- 缓存积分余额（如需要）
- 批量处理大量用户操作

### 3. 扩展性考虑
- 支持多种积分类型
- 积分过期策略
- 积分转赠功能
- 积分兑换商城

## 🔧 故障排除

### 常见问题

#### 1. 积分未正确发放
检查项:
- Stripe webhook 是否正确配置
- 事件是否重复处理
- 用户是否有积分账户

#### 2. 数据库连接问题
检查项:
- DATABASE_URL 是否正确
- 数据库权限是否充足
- 网络连接是否正常

#### 3. 前端显示问题
检查项:
- Server Actions 是否正确返回数据
- 组件状态管理是否正确
- 错误处理是否完善

## 📚 相关文档

- [Better Auth 文档](https://better-auth.com)
- [Drizzle ORM 文档](https://orm.drizzle.team)
- [Stripe Webhooks 文档](https://stripe.com/docs/webhooks)
- [Next.js App Router 文档](https://nextjs.org/docs/app)

## 🤝 贡献指南

1. 创建功能分支
2. 添加测试用例
3. 更新文档
4. 提交 Pull Request

---

*最后更新: 2024年12月*
